train:
  test_size: 0.2
  random_state: 42
  models:
    linear_regression: {}
    random_forest:
      n_estimators: 100
      max_depth: 10
    gradient_boosting:
      n_estimators: 100
      learning_rate: 0.1

preprocessing:
  target: price
  features: [area, bedrooms, bathrooms, stories, parking]
